server:
  port: 8083

spring:
  application:
    name: order-tracking-service
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 1
    consumer:
      group-id: order-tracking-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.storex.ordertracking.model
        spring.json.value.default.type: com.storex.ordertracking.model.OrderTrackingEvent
    listener:
      concurrency: 5

storex:
  kafka:
    order-tracking-topic: order-tracking

